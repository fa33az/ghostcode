image: node:20

pipelines:
  default:
    - step:
        name: Ghost Code Static Analysis
        script:
          - npm ci
          - npm run build
          - npx @fa33az/ghostcode --threshold 70 --orphans --fail-on-high 0
        artifacts:
          - ghostcode-report.html
